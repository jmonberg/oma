<div class = "container">
<h1>Michigan Avenue Profiles</h1>

<p>Share something you've experienced or a memory associated with Michigan Avenue, and read other people's stories.</p>

<table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th>Body</th>
      <th>Location</th>
      <th>Author</th>


      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

    <% @profiles.each do |profile| %>
      <tr>
        <td><%= profile.title %></td>
        <td><%= textilize profile.body %></td>
        <td><%= profile.location %></td>
        <td><%= profile.user.display_name %></td>

        <td><%= link_to 'Show', profile %></td>
        <td><%= link_to 'Edit', edit_profile_path(profile) %></td>
        <td><%= link_to 'Destroy', profile, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>

     </tbody>
</table>
<%= will_paginate @profiles %>
<br>

<%= link_to 'New Profile', new_profile_path %>
</div>
